---
title: Mobx
date: 2020-01-01
draft: false
slug: mobx
categories: state-management
tags:
  - debug
  - state
lastmod: 2026-05-26T00:00:00.000Z
question: How do you debug unexpected MobX re-renders and infinite update loops?
summary: MobX's trace() helper shows exactly which observable changes triggered a reaction, making it easier to track down unexpected renders or infinite loops. Wrap components with trace() during development to log the dependency chain that caused each update.
description: MobX debugging tips — using trace() to find unexpected renders and infinite update loops.
---

## Debugging

### trace()

`trace()` helps show what it was that made MobX update. this is helpful for tracking down unexpected renders or infinite loops.

```jsx
import {trace} from 'mobx-react'

{...}
render() {
  trace();
  return ();
}
```

## toJS()

Each observable has a method of `toJS()` on it. May need to look into typing them correctly.
