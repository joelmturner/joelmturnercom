---
// basic tracking only - load asynchronously and skip bots
---

<script is:inline>
  // skip tracking for bots
  const userAgent = typeof navigator !== 'undefined' ? navigator.userAgent : '';
  const isBot = /bot|crawler|spider|crawling/i.test(userAgent);

  if (!isBot) {
    (function (t, e) {
      var o, n, p, r;
      // @ts-ignore - PostHog global type
      if (!('__SV' in e) && (!window.posthog || !window.posthog.__loaded)) {
        // @ts-ignore - PostHog global type
        window.posthog = e;
        ((e._i = []),
          (e.init = function (i, s, a) {
            function g(t, e) {
              var o = e.split('.');
              (2 == o.length && ((t = t[o[0]]), (e = o[1])),
                (t[e] = function () {
                  t.push([e].concat(Array.prototype.slice.call(arguments, 0)));
                }));
            }
            (((p = t.createElement('script')).type = 'text/javascript'),
              (p.crossOrigin = 'anonymous'),
              (p.async = !0),
              (p.src =
                s.api_host.replace('.i.posthog.com', '-assets.i.posthog.com') + '/static/array.js'),
              (r = t.getElementsByTagName('script')[0]),
              r && r.parentNode && r.parentNode.insertBefore(p, r));
            var u = e;
            for (
              void 0 !== a ? (u = e[a] = []) : (a = 'posthog'),
                u.people = u.people || [],
                u.toString = function (t) {
                  var e = 'posthog';
                  return ('posthog' !== a && (e += '.' + a), t || (e += ' (stub)'), e);
                },
                u.people.toString = function () {
                  return u.toString(1) + '.people (stub)';
                },
                o =
                  'capture identify alias set_config opt_out_capturing has_opted_out_capturing opt_in_capturing reset'.split(
                    ' '
                  ),
                n = 0;
              n < o.length;
              n++
            )
              g(u, o[n]);
            e._i.push([i, s, a]);
          }),
          (e.__SV = 1));
      }
      // @ts-ignore - PostHog global type
    })(document, window.posthog || []);
    // @ts-ignore - PostHog global type
    posthog.init('phc_rw4uuoOb20piH4Ze3ATBBnoqxQpadrV5AEwjfnw7lup', {
      api_host: 'https://us.i.posthog.com',
      person_profiles: 'identified_only',
      session_recording: false,
      disable_surveys: true,
      disable_session_recording: true,
      capture_pageview: true,
      capture_pageleave: false,
    });
  }
</script>
