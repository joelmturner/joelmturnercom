---
type Props = {
  link?: string;
};

const { link } = Astro.props;
---

{
  link ? (
    <a href={link} style="text-decoration: none;">
      <wa-badge>
        <slot />
      </wa-badge>
    </a>
  ) : (
    <wa-badge>
      <slot />
    </wa-badge>
  )
}
