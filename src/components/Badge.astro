---
type Props = {
  link?: string;
};

const { link } = Astro.props;
---

{
  link ? (
    <wa-button href={link} appearance="filled-outlined" variant="brand">
      <slot />
    </wa-button>
  ) : (
    <wa-badge appearance="filled-outlined">
      <slot />
    </wa-badge>
  )
}
