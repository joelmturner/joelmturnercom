---
import { cn } from '@lib/utils';

type Props = {
  className?: string;
};

const { className } = Astro.props;
---

<div
  class={cn(
    'prose dark:prose-invert prose-h1:font-bold prose-h1:text-xl prose-img:rounded-xl w-full max-w-full',
    className
  )}
>
  <slot />
</div>
