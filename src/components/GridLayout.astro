---
import PageTitle from './PageTitle.astro';

type Props = {
  title?: string;
};

const { title } = Astro.props;
---

<div class="flex flex-col gap-8">
  {title && <PageTitle title={title} />}
  <div class="grid grid-cols-1 md:grid-cols-2 gap-10 m-0 p-0">
    <slot />
  </div>
</div>
