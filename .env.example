# copy to the collection root (parent of main/), then edit:
#   cp .env.example ../.env
#
# bootstrap-worktree.sh symlinks ../.env into each worktree on checkout.

PUBLIC_CLOUDINARY_CLOUD_NAME=your-cloud-name
PUBLIC_SENTRY_DSN=
SENTRY_AUTH_TOKEN=
